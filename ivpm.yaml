package:
    name: simscripts

    dep-sets:
    - name: default
      deps: []

    - name: default-dev
      deps: []

    paths:
      export:
        lib-dirs:
          - verilog/rtl
      project:
        lib-dirs:
          - verilog/rtl
